<nav class="navbar navbar-inverse navbar-fixed-top">

    <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="index.php?page=home">{{ title }}</a>
    </div>

    <div class="collapse navbar-collapse" id="myNavbar">
        <ul class="nav navbar-nav">

            <!-- Polozky menu, mohou byt aktivni -->
            <li {% if index == 1 %} class="active" {% endif %}> <a href="index.php?page=home">Home</a></li>
            <li {% if index == 2 %} class="active" {% endif %}> <a href="index.php?page=about">About</a></li>
            <li {% if index == 3 %} class="active" {% endif %}> <a href="index.php?page=contacts">Contacts</a></li>

        </ul>
        <ul class="nav navbar-nav navbar-right">
            <!-- Pokud je uzivatel prihlasen-->
            {% if user is defined %}

                <!-- Pokud neni prihlasen uzivatel ale je vybrana stranka prihlaseni -->
            {% elseif index == 0 %}
                <li class="active">
                    <a href="index.php?page=login">
                        <span class="glyphicon glyphicon-log-in"></span> Log in</a>
                </li>
                <!-- Jinak -->
            {% else %}
                <li>
                    <a href="index.php?page=login">
                        <span class="glyphicon glyphicon-log-in"></span> Log in</a>
                </li>
            {% endif %}

        </ul>
    </div>

</nav>